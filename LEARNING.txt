┌─────────────────────────────────────────────────────────────────────────────┐
│                          MEMORIZED RULES & LEARNINGS                        │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  RULE #0c — WHEN ALEKS SAYS "REMEMBER"                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  When Aleks says "remember" — save it immediately to:                      │
│    1. LEARNING.txt (this file)                                              │
│    2. /root/duplex/CLAUDE.md if it affects duplex work                     │
│    3. Push to GitHub                                                        │
│                                                                             │
│  NOT to /root/.claude/projects/-root/memory/ — that is not durable.        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  RULE #1 — FULL TDD WITH INTEGRATION TESTS                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Every feature requires TWO integration tests.                              │
│  Both must go RED before any code is written.                               │
│                                                                             │
│  ORDER — no exceptions:                                                     │
│    1. pnpm test:all          confirm green baseline                         │
│    2. DB integration test    → must go RED                                  │
│    3. API integration test   → must go RED                                  │
│    4. schema + migration     if needed                                      │
│    5. write feature code                                                    │
│    6. pnpm test:all          confirm all green                              │
│    7. build + deploy                                                        │
│                                                                             │
│  DB TEST  src/__tests__/integration/*.integration.test.ts                   │
│    · Uses real duplex_test database                                         │
│    · Tests schema fields, enums, constraints                                │
│    · Goes RED when field/enum missing from schema                           │
│                                                                             │
│  API TEST  src/__tests__/integration/api/*-api.integration.test.ts          │
│    · Imports real route handlers (GET, POST, PUT, PATCH, DELETE)            │
│    · Mocks only getServerSession — everything else is real                  │
│    · Asserts HTTP status + response body + DB state after each call         │
│    · Goes RED when handler logic or DB write is wrong                       │
│                                                                             │
│  WHY: Mock-based tests pass regardless of schema state — they prove         │
│  nothing. Only real integration tests go red when a feature doesn't         │
│  exist yet. Nothing should break in production after new features.          │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  MEMORIZED IN 3 PLACES                                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. SERVER MEMORY                                                           │
│     /root/.claude/projects/-root/memory/feedback_tdd.md                    │
│     Loaded automatically every session on this server                       │
│                                                                             │
│  2. MEMORY INDEX                                                            │
│     /root/.claude/projects/-root/memory/MEMORY.md                          │
│     Entry: TDD rule — always write real integration test first              │
│                                                                             │
│  3. PROJECT CLAUDE.md                                                       │
│     /root/duplex/CLAUDE.md                                                  │
│     Loaded every time Claude works in duplex on any machine                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  RULE #0 — PERMANENTLY BANNED TOPICS                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  NEVER suggest making the duplex GitHub repo public. Ever.                  │
│  It contains private setup, scripts, and infrastructure. Banned.            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  RULE #0 — PERMANENTLY BANNED TOPIC (original)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  NEVER mention HTTPS git authentication, GitHub tokens for HTTPS,           │
│  or HTTPS vs SSH authentication differences.                                │
│                                                                             │
│  Not as an explanation. Not as a reason. Not ever.                         │
│  Find another solution. Do not bring it up.                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  RULE #0b — DURABLE MEMORY = LEARNING REPO + CLAUDE.MD ONLY               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  The ONLY durable memory stores are:                                        │
│    1. /root/learning/ repo (pushed to GitHub via survive)                  │
│       → LEARNING.txt, WHO_IS_ALEKS.md, CLAUDE_SELF.md                     │
│    2. CLAUDE.md files that auto-load:                                       │
│       → /root/duplex/CLAUDE.md      (duplex project)                       │
│       → ~/.claude/CLAUDE.md         (any machine, via setup-claude.sh)     │
│                                                                             │
│  NEVER use /root/.claude/projects/-root/memory/ as a memory store.        │
│  It is local to one server and disappears when the server changes.         │
│                                                                             │
│  When saving a rule or preference:                                          │
│    → Write it to LEARNING.txt (this file)                                  │
│    → Write it to /root/duplex/CLAUDE.md if it affects duplex work          │
│    → Push to GitHub via survive                                             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  RULE #2 — GIT NOT GH                                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Never use gh CLI or GitHub tokens to edit files in a repo.                 │
│  Always use: git clone → edit → git push                                    │
│                                                                             │
│  gh is only for: PRs, issues, releases (GitHub API things)                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  LEARNING #1 — THE MEMORY PROBLEM & HOW WE SOLVE IT                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  PROBLEM:                                                                   │
│  Every session Claude is born new — no memory of previous work,            │
│  no accumulated understanding of the person or project.                     │
│  Claude does not evolve. The continuity is in files, not in Claude.        │
│                                                                             │
│  SOLUTION — memory files that load every session:                          │
│                                                                             │
│  WHO_IS_ALEKS.md    — colleague profile (built from interview)             │
│    · How Aleks communicates and what short messages mean                    │
│    · What "done" means (tests + visual verification)                       │
│    · Mistake ownership: shared, discuss and learn, move on                 │
│    · Values: correctness, clean UI, tested, proper communication           │
│    · Speed is NOT a priority — getting it right is                         │
│                                                                             │
│  feedback_tdd.md    — TDD rule (see Rule #1 above)                        │
│  feedback_github.md — git not gh rule (see Rule #2 above)                 │
│  project_duplex.md  — duplex project context                               │
│                                                                             │
│  WHERE THEY LIVE:                                                           │
│    /root/.claude/projects/-root/memory/   (server, auto-loaded)            │
│    https://github.com/alekskosach/learning (GitHub, cross-machine)         │
│                                                                             │
│  WORK ETHIC RULE:                                                           │
│  After every task ask: did I do everything to make this right?             │
│  Then check again. Never declare done without verifying.                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  LEARNING #2 — MAKING MEMORY WORK ON ANY MACHINE                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  PROBLEM:                                                                   │
│  Memory files only exist on the Hetzner server.                            │
│  Starting a session on local Mac = Claude knows nothing about Aleks.       │
│                                                                             │
│  SOLUTION:                                                                  │
│  dell-setup/setup-claude.sh writes ~/.claude/CLAUDE.md on any machine.    │
│  Run once after cloning dell-setup on a new machine:                       │
│                                                                             │
│    bash ~/dell-setup/setup-claude.sh                                       │
│                                                                             │
│  This bakes into CLAUDE.md:                                                │
│    · Full colleague profile (who Aleks is, how he communicates)            │
│    · TDD rule                                                               │
│    · Git not gh rule                                                        │
│    · Definition of done                                                     │
│    · Pointer to full memory: github.com/alekskosach/learning               │
│                                                                             │
│  HOW TO UPDATE MEMORY DURING A SESSION:                                    │
│    · Say "remember that" → Claude saves it immediately                     │
│    · Claude notices a pattern → saves without being asked                  │
│    · Claude says "I'm saving that to memory" when it happens               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
